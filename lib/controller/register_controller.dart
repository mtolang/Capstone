import 'package:flutter/material.dart';

class RegisterParentUser {
  void registerParentUser(
    BuildContext context,
    String fullName,
    String userName,
    String email,
    String contactNumber,
    String address,
    String password,
    String passwordConfirm,
  ) {
    // Design only: No backend, no navigation, no dialogs
  }
}

class RegisterTherapistUser {
  void registerTherapistUser(
    BuildContext context,
    String fullName,
    String userName,
    String email,
    String contactNumber,
    String address,
    String password,
    String passwordConfirm,
  ) {
    // Design only: No backend, no navigation, no dialogs
  }
}

class RegisterClinicUser {
  void registerClinicUser(
    BuildContext context,
    String clinicName,
    String userName,
    String email,
    String contactNumber,
    String address,
    String password,
    String passwordConfirm,
  ) {
    // Design only: No backend, no navigation, no dialogs
  }
}
